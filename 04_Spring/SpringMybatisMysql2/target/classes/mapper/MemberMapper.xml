<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace ="com.mulcam.myapp.dao.MemberDAO">
  <select id ="login" resultType = "com.mulcam.myapp.vo.MemberVO"><!-- dao에서 정의해준 메소드 명 mybatis가 알아서 담아줌-->
  	 select userid, username from member where userid=#{userid} and userpwd=#{userpwd}
  </select>
  </mapper>